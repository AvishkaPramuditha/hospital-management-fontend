export class Patient {
    constructor(public id: number, public name: string, public address: string, public dob: Date, public gender: string, public email: string, public contactNumber: string, public emergencyContactNumber: string, public bloodType: string, public allergies: string, public cause: string, public priorityLevel: number) {   

    }
}
